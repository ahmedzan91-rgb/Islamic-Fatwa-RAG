# ============================================================================
#  متطلبات التشغيل المحلي بالجودة الكاملة (تضمين عصبي دلالي)
#  ---------------------------------------------------------------------------
#  التثبيت:  pip install -r requirements-full.txt
#  بايثون:   3.9 → 3.12  (torch لا يدعم 3.13/3.14 بعد)
#
#  يشمل كل ما في requirements.txt بالإضافة إلى الطبقة العصبية.
#  استخدم هذا الملف لبناء الفهرس محلياً ثم ارفع مجلد chroma_db.
# ============================================================================

# ------------------------- الأساس (نفس النشر السحابي) -----------------------
streamlit>=1.36.0
pandas>=2.0.0
numpy>=1.24.0,<2.3.0
chromadb>=0.5.0
scikit-learn>=1.3.0
requests>=2.31.0

# ------------------------ تسريع القراءة والحفظ ------------------------------
pyarrow>=14.0.0            # ملفات Parquet الوسيطة (أسرع وأصغر من CSV)

# ------------------- الطبقة العصبية (التضمين الدلالي) -----------------------
# ملاحظة: على أنظمة بلا كرت رسومات، ثبّت نسخة CPU لتوفير ~2 GB:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0
sentence-transformers>=3.0.0
transformers>=4.40.0
huggingface-hub>=0.23.0

# ------------------------------ أدوات مساعدة -------------------------------
tqdm>=4.66.0

# ============================================================================
#  النماذج الموصى بها للعربية
# ----------------------------------------------------------------------------
#  الافتراضي (جودة عالية، 768 بُعد، ~1.1 GB):
#      intfloat/multilingual-e5-base
#
#  الخفيف (جودة جيدة، 384 بُعد، ~450 MB):
#      export ISLAMIC_RAG_EMBEDDING_MODEL="sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
#
#  فرض واجهة معيّنة:
#      export ISLAMIC_RAG_BACKEND=neural   # أو tfidf أو auto (الافتراضي)
# ============================================================================
