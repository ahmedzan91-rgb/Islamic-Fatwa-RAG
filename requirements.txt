# ============================================================================
#  متطلبات النشر السحابي (Streamlit Community Cloud)
#  ---------------------------------------------------------------------------
#  هذا الملف خفيف عمداً: لا يحتوي torch ولا sentence-transformers.
#  السبب: Streamlit Cloud يفرض حدّ ذاكرة 1 GB ويستخدم Python 3.14
#         التي لا تتوفر لها wheels لـ torch — فيفشل البناء أو يُستنزف.
#
#  النظام يكتشف غياب المكتبات العصبية تلقائياً ويستخدم واجهة TF-IDF الخفيفة.
#
#  ▸ للتشغيل المحلي بالجودة الكاملة (تضمين عصبي):
#        pip install -r requirements-full.txt
# ============================================================================

# ------------------------- الواجهة التفاعلية -------------------------------
streamlit>=1.36.0

# --------------------- معالجة البيانات وقراءة CSV --------------------------
pandas>=2.0.0
numpy>=1.24.0

# ------------------------ قاعدة البيانات المتجهة ---------------------------
chromadb>=0.5.0

# ------------------- التضمين الخفيف (بلا torch) ----------------------------
scikit-learn>=1.3.0

# ------------------------ الاتصال بـ OpenRouter -----------------------------
requests>=2.31.0
