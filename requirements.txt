# ============================================================================
# متطلبات نظام الـ RAG الإسلامي للإجابة على الفتاوى والمسائل الشرعية
# التثبيت:  pip install -r requirements.txt
# بايثون المطلوب: 3.9 → 3.12  (يُنصح بـ 3.11 للنشر على Streamlit Cloud)
# ============================================================================

# ------------------------- الواجهة التفاعلية -------------------------------
streamlit>=1.36.0,<2.0.0

# --------------------- معالجة البيانات وقراءة CSV --------------------------
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.3.0
pyarrow>=14.0.0            # لحفظ/قراءة ملفات Parquet الوسيطة

# ------------------------ قاعدة البيانات المتجهة ---------------------------
chromadb>=0.5.0,<0.7.0

# ------------------- نماذج التضمين الداعمة للعربية -------------------------
sentence-transformers>=3.0.0
torch>=2.0.0               # على Streamlit Cloud استخدم نسخة CPU (انظر الملاحظة أدناه)
transformers>=4.40.0
huggingface-hub>=0.23.0

# ------------------- الوضع الاحتياطي والمقاييس ------------------------------
scikit-learn>=1.3.0        # TF-IDF fallback + مقاييس التقييم

# ------------------------ الاتصال بـ OpenRouter -----------------------------
requests>=2.31.0

# ------------------------------ أدوات مساعدة -------------------------------
tqdm>=4.66.0
python-dotenv>=1.0.0       # للتشغيل المحلي فقط (لا يُستخدم لتخزين مفاتيح الإنتاج)

# ============================================================================
# ملاحظات النشر على Streamlit Cloud:
# ----------------------------------------------------------------------------
# 1) لتقليل حجم البناء وتفادي تجاوز حدّ الذاكرة، استبدل سطر torch أعلاه بـ:
#       --extra-index-url https://download.pytorch.org/whl/cpu
#       torch==2.3.1+cpu
#
# 2) إذا واجهت مشاكل في بناء chromadb، أضف ملف packages.txt يحوي:
#       build-essential
#
# 3) نموذج التضمين الخفيف الموصى به عند محدودية الموارد (384 بُعد):
#       sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
#    يُضبط عبر متغير البيئة: ISLAMIC_RAG_EMBEDDING_MODEL
#
# 4) المفاتيح تُضاف من Settings → Secrets ولا تُكتب في أي ملف داخل المستودع.
# ============================================================================
